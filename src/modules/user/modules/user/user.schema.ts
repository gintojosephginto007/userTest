import Joi from '@hapi/joi';
import i18n from '../../../../config/i18n';
import { StringInput, tableDefaultParams, NumberInput, DateRangeAlternatives, ValidateDateRange } from '../../../../common/validators/joi-custom-schema';

export const UserSchema = {
}
